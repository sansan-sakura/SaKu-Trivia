// import styles from "./Card.module.scss";
// export const Card = ({ item, onChoose, isChosen, style }) => {
//   return (
//     <li
//       key={item.id}
//       className={styles.btn_wrapper}
//       style={{
//         outline: isChosen && item.id === category ? "#fbbf24 4px solid" : "",
//         outlineOffset: isChosen && item.id === category ? "2px" : "",
//       }}
//     >
//       <button onClick={() => handleChoose(cate.id)} value={cate.id}>
//         {cate.name}
//       </button>
//     </li>
//   );
// };
